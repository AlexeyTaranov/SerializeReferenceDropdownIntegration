name: Version Bump Pull Request

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  version-bump:
    runs-on: ubuntu-latest
    name: Version Bump PR

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 🔹 Determine new version and changelog
      - name: Determine new version and changelog
        id: changelog
        uses: googleapis/release-please-action@v4
        with:
          release-type: simple
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Stop if no new release
        if: ${{ steps.changelog.outputs.release_created != 'true' }}
        run: echo "No new version to release."

      # 🔹 Update gradle.properties version
      - name: Update gradle.properties
        if: ${{ steps.changelog.outputs.release_created == 'true' }}
        run: |
          TAG_NAME="${{ steps.changelog.outputs.tag_name }}"
          VERSION="${TAG_NAME#v}"  # remove leading 'v'
          sed -i.bak "s/^version=.*/version=${VERSION}/" gradle.properties
          echo "✅ Updated gradle.properties to version ${VERSION}"

      # 🔹 Update plugin.xml version
      - name: Update plugin.xml
        if: ${{ steps.changelog.outputs.release_created == 'true' }}
        run: |
          TAG_NAME="${{ steps.changelog.outputs.tag_name }}"
          VERSION="${TAG_NAME#v}"
          XML_FILE="src/rider/main/resources/META-INF/plugin.xml"
          echo "🧩 Updating ${XML_FILE} to version ${VERSION}"

          awk -v ver="$VERSION" '
            /<!--/ { in_comment=1 }
            /-->/ { in_comment=0 }
            !in_comment {
              gsub(/<version>[^<]+<\/version>/, "<version>" ver "</version>")
            }
            { print }
          ' "$XML_FILE" > "$XML_FILE.tmp" && mv "$XML_FILE.tmp" "$XML_FILE"

          echo "✅ Updated version tags in plugin.xml:"
          grep "<version>" "$XML_FILE" || echo "(none found)"

      # 🔹 Build the plugin (before PR)
      - name: Build plugin
        if: ${{ steps.changelog.outputs.release_created == 'true' }}
        run: ./gradlew buildPlugin

      # 🔹 Upload ZIP as artifact (optional, for PR preview)
      - name: Upload plugin artifact
        if: ${{ steps.changelog.outputs.release_created == 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: plugin-build
          path: build/distributions/*.zip

      # 🔹 Create pull request with version bump
      - name: Create Pull Request
        if: ${{ steps.changelog.outputs.release_created == 'true' }}
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "chore(release): bump version to ${{ steps.changelog.outputs.tag_name }}"
          branch: "release-bump-${{ steps.changelog.outputs.tag_name }}"
          title: "Bump plugin version to ${{ steps.changelog.outputs.tag_name }}"
          body: |
            This PR updates the plugin version to `${{ steps.changelog.outputs.tag_name }}`  
            Changes include:
            - gradle.properties version
            - plugin.xml version
            - CHANGELOG.md

            The plugin ZIP has been built and is attached as an artifact.
          labels: release, version-bump
          base: main
          delete-branch: true
          token: ${{ secrets.GITHUB_TOKEN }}
